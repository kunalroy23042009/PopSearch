{
  "name": "creator-content-radar-frontend",
  "private": true,
  "scripts": {
    "build": "esbuild app.jsx --bundle --minify --target=es2020 --external:react --external:react-dom --external:chart.js --outfile=app.js"
  }
}
